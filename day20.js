// The global variable
let fixedValue = 4;
//function that increase the value of a variable by 1 without changing the value
function incrementer() {

  let increase = 0;
  return increase = fixedValue + 1;

  // Only change code above this line
}